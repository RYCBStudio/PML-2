# Changelog

本文档记录 PML 2（MEFrpLauncherX）主要版本的功能变更。应用内「更新」页的更新日志以服务端发布信息为准。

## 26.3.1（开发中）

> 主题：**插件实用化——事件更全、动作更强、表达式可算、看得见执行。**

### 插件系统（P0）

- **`proxy.failed` 事件**（S1）：隧道启动失败/运行异常时触发，携带 `proxyName` / `errorMessage` / `errorCategory`（认证失败 / 启动超时 / 接口错误 / 其他），支持失败自动恢复类插件。
- **`proxy.restart` 动作**（S2）：按隧道名先关闭再启动，重启能力经静态委托桥注入引擎（UI 线程安全），插件可做「失败自动重启」自愈。
- **条件表达式四则运算与括号**（S3）：`+ - * /` 与 `()`，支持 `-and/-or/-not`、`&&/||`、全部比较运算符，`ctx.data` / `ctx.variables` 取值；数值比较类型归一（`9 == 9.0`）。
- **内置函数库 v1**（M5）：条件表达式与动作参数模板支持 `len` / `lower` / `upper` / `coalesce` / `min` / `max` / `now`；新增 `open_url` 动作（默认浏览器打开链接）；参数模板支持内嵌多个 `{{...}}` 与函数调用。
- **执行日志**（S4）：插件页新增「执行日志」Tab，实时展示触发/动作执行结果（命中 / 跳过 / 成功 / 失败），内存上限 200 条，可清空。
- **表单式插件编辑器**（S6）：新建/编辑插件不再手写 YAML——下拉选事件与动作、表单填参数、实时预览与校验；事件/动作下拉与运行时引擎同源（注册表驱动）。
- **官方示例**（S5）：`docs/plugin-examples/` 提供 `proxy-guard.yaml`（失败自动重启）、`proxy-notify.yaml`（启动/失败通知）与 `demo-functions.yaml`（函数/算术/open_url）。

### 启动与桌面（P1）

- **Splash 进度管道**（M1）：独立 Splash 窗口显示启动进度条与阶段文案，主程序经命名管道（`tech.rycb.pml2.splash.{pid}`）推送，与单实例激活管道分离。
- **Splash 主题样式**（M2）：设置 → 外观可开关启动画面并选择内置样式（默认 / 深色 / 简约）或自定义背景图，更改后下次启动生效。
- **macOS 菜单重组**（M3）：原生菜单扩展为六项（应用 / 文件 / 隧道 / 节点 / 查看 / 帮助），新增打开日志目录、刷新隧道列表、节点监控、流量悬浮窗开关、官方文档、检查更新，全部命令与托盘/主界面同源。

### 文档

- 更新 `docs/插件系统埋点说明.md`（proxy.failed 事件、proxy.restart / open_url / local_run 动作、条件表达式与函数库、模板变量、执行日志）、`docs/user_guide.md`（插件编辑器、Splash 样式、macOS 菜单）。

---

## 26.3.0（2026-08）

> 主题：**选得准、开得快、挂了能懂；更新可靠；桌面集成可用。**
> 详细用户说明见 [docs/user_guide.md](docs/user_guide.md)，排障见 [docs/troubleshooting.md](docs/troubleshooting.md)。

### 隧道（P0）

- **节点延迟 / 连通性**（M1）：隧道管理页每张卡片显示所在节点延迟（TCP 探测 `节点地址:远程端口`）；新增「刷新测速」批量探测，并发受控不卡界面，可取消；超时（4 秒）/失败/不可探测均有明确文案。
- **创建隧道预填与模板**（M2）：本地地址默认预填 `127.0.0.1`（可配置）；常用端口快捷项 80 / 443 / 22 / 25565；支持「套用 / 存为 / 删除」模板，持久化于 `Settings.json`，重启仍在。
- **隧道状态与失败原因**（M3）：统一状态机（启动中 / 运行中 / 重连中 / 已停止 / 失败 / 空闲）；启动失败映射为可读文案（认证失败 / 端口占用 / 节点不可达 / 进程崩溃）；「复制错误信息」一键复制应用版本 + mefrpc 版本 + 失败摘要。

### 更新与分发（P0）

- **更新页失败可恢复**（M4）：下载失败/校验失败不再静默，展示明确文案与「重试下载」按钮；检查更新失败有原因提示；下载源（TPCA/官方）切换指引。

### 桌面集成（P1）

- **macOS 原生菜单**（M5）：应用菜单（关于 / 设置 / 退出）与隧道菜单（管理 / 创建 / 显示主窗口 / 全部停止隧道），命令与主界面同源；关窗与退出的行为已文档化。
- **系统托盘**（M6）：托盘菜单增加「全部停止隧道」；显示主窗口 / 退出均与主界面状态一致。
- **流量悬浮窗打磨**（M6b）：`⋯` 菜单三项（刷新流量 / 设置 / 关闭）绑定真实命令；位置持久化；应用退出时自动销毁，无残留置顶窗。
- **开机自启与恢复隧道**（M7）：Windows（注册表）/ macOS（LaunchAgent）/ Linux（autostart）三平台自启；「恢复上次隧道」默认关闭；启动失败写日志并提示。

### 其他

- 文档：新增/更新 `docs/user_guide.md`、`docs/install.md`、`docs/troubleshooting.md`、`docs/26.3-entry-map.md`。

---

## 26.2（历史版本）

- 插件系统（事件触发 / 本地运行 / 埋点）、多语言 i18n、主题编辑器、工具箱等。详见 Git 历史。
