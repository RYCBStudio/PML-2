# 26.3 版本入口清单（S0–S4 工作地图）

> 26.3 迭代 S0–S4 的功能落点速查表。分支 `release/26.3`（基于 dev 主线）。
> 本文档供开发与 Agent 定位代码使用，随版本更新。

## 数据与模型

| 关注点 | 代码位置 |
|---|---|
| 节点数据源/缓存 | `MEFrpLauncherX.Core/MEFIntegrated/MEFApiConverter.cs`（`EnsureNodesStatusInfoAsync` / `EnsureNodesListInfoAsync` / `CurrentNodesListInfo` / `CurrentNodesStatusInfo`） |
| 节点列表模型 | 创建页：`InfoClasses.NodeInfo`（含 `hostname`、`servicePort`）、`InfoClasses.NodeStatus`（无 hostname）——**API 不暴露节点 IP，不可用于延迟探测**；隧道管理页：`InfoClasses.Nodes`（nodeId/name/**hostname**，`GetProxiesAsync` 返回，已用于拼 `location`） |
| 隧道列表模型 | `InfoClasses.Proxies`（proxyId/proxyName/remotePort/isOnline 等） |

## 界面入口

| 界面 | 代码位置 |
|---|---|
| 选节点 VM（创建页） | `ViewModels/NodesContainerViewModel.cs`（`LoadNodesAsync` 构建 `TunnelNodeViewModel`） |
| 节点卡片 VM（创建页） | `ViewModels/Controls/TunnelNodeViewModel.cs` |
| 节点监控 | `ViewModels/NodesOverviewViewModel.cs` + `Views/NodesMonitoringPage.axaml` |
| 创建隧道表单 | `Controls/CreateProxy.axaml` + `CreateProxy.axaml.cs`（含 `CreateProxyViewModel`，`LocalAddress` 默认 `127.0.0.1`（M2 预填）；校验在 `CreateProxy_OnCreateProxy`） |
| 创建入口/容器 | `Views/CreateProxyPage.axaml`（Tab：引导版/专家版/嘉豪版）+ `ViewModels/CreateProxyPageViewModel.cs`。引导版为四步向导（`GuideStage`：类型页→候选节点容器 `GuidePick`→创建表单→提交），模板条目来自 `create-proxy-template` 类型插件的 `templates` 段（内置 `Config/Plugins/create-proxy-template.yaml`，多插件合并、支持禁用/热重载），不再硬编码；选完模板展示筛选后的候选节点由用户点选（不复用专家模式选中态） |
| 隧道卡片/状态（隧道管理页） | `ViewModels/UserProxyViewModel.cs`（`isOnline` API 状态、`IsLaunched` 本地状态、`IsLoading`；`LaunchSingleProxy` / `StopProxy`；`Node.hostname` 可用，**M1 延迟探测落点**） |
| 隧道列表加载 | `Views/ManageProxyPage.axaml.cs` `LoadProxies()`（`GetProxiesAsync` 构建 `UserProxyViewModel`） |
| 隧道列表 VM | `ViewModels/ManageProxyViewModel.cs`（`FilteredProxies` / `AllProxies` / `FilterProxies`） |
| 隧道启停命令 | `Views/TerminalPage.axaml.cs` `CreateNewTerminalWithoutNotification` / `SendCtrlCCommandToSelected` |
| 错误展示控件（已有） | `Controls/TunnelErrorPresenter.axaml(.cs)` |
| 更新页 | `Views/UpdatePage.axaml` + `ViewModels/UpdatePageViewModel.cs`（`CheckUpdate` / `DownloadUpdate` / `RetryDownloadCommand` / `HasDownloadFailed`；`BuildUpdateDownloadUrl` 拼链接） |
| 托盘 | `Views/MainWindow.axaml.cs`（`_notifyIcon`、`CreateContextMenu`：打开主界面/打开终端/全部停止隧道/退出，均走 `AppShellCommands`） |
| macOS 原生菜单（M5） | `MainWindow.axaml.cs` `OnLoaded` 中 `NativeMenuBar` 段（应用菜单：关于/设置/退出；隧道菜单：管理/创建/显示主窗口/停止全部隧道） |
| 悬浮窗 | `Views/ProxyMonitor/ProxyFloat.axaml` + `ProxyFloatSettings.axaml`；`⋯` 菜单三项绑定 `RefreshTrafficCommand`/设置/关闭；`App.axaml.cs` `desktop.Exit` 销毁悬浮窗 |
| 开机自启（M7） | `Views/SettingsPage.axaml.cs`（`AutoStartChanged` + `SetAutoStartWindows` / `SetAutoStartLinux` / `SetAutoStartMacOS`）；`MainWindow.axaml.cs` AUTO_START 段 try-catch |
| 共享命令（S3.0） | `MEFrpLauncherX/Services/AppShellCommands.cs`（`IAppShellCommands`：ShowMainWindow/ExitApplication/OpenSettings/StopAllTunnelsAsync/RefreshFloatTraffic） |

## 基础设施

| 关注点 | 代码位置 |
|---|---|
| 配置持久化 | `MEFrpLauncherX.Core/ConfigManager.cs` + `Config/Settings.json`（JSON 序列化，含 `PMSettings`/`UpdateSettings` 等） |
| i18n | `MEFrpLauncherX.Core/Languages/`（resx + 三语 Designer，全部走 `Languages.*` 静态属性） |
| AOT 序列化登记 | `MEFrpLauncherX.Core/AppJsonSerializerContexts.cs` / `MEFrpLauncherX/AppJsonSerializerContexts.cs` |
| 导航 | `ViewModels/MainPageFrameViewModel.cs` `NavigateToPage("Manage"/"Create"/"Update"…)` |
| frpc 下载/校验 | `UserProxyViewModel.IsClientFileValidAsync` / `DownloadHelper` |
| 探测服务（M1 新增） | `MEFrpLauncherX.Core/Services/NodeProbeService.cs`（`INodeProbeService` / `NodeProbeService`，TCP Connect，超时 4s，并发 6） |

## 26.3 里程碑落点

- **M1 节点延迟/连通性**（落点：隧道管理页）：`UserProxyViewModel`（探测属性/方法）、`ManageProxyViewModel`（`ProbeAllCommand`）、`UserProxyControl.axaml`（延迟展示）、`ManageProxyPage.axaml`（刷新测速按钮）、`Core/Services/NodeProbeService.cs`（TCP Connect，超时 4s，并发 6）。
- **M2 创建隧道预填与模板**（落点：创建页）：`CreateProxy.axaml(.cs)`（`127.0.0.1` 预填/端口快捷 80/443/22/25565/模板 UI）、`ConfigManager`（`ProxyTemplates` 持久化）、`Models/ProxyTemplate.cs`（已登记 AOT）。
- **M3 隧道状态与失败原因**（落点：隧道管理页 + 终端）：`UserProxyViewModel`（`TunnelStatus` 状态机/`LastErrorSummary`）、`TerminalPage.CreateNewTerminalWithoutNotification`（输出回调）、`Core/Services/TunnelErrorMapper.cs`（认证失败/端口占用/节点不可达/进程崩溃）、`TunnelErrorPresenter`。
- **M4 更新页与失败路径**（落点：更新页）：`UpdatePageViewModel`（`DownloadUpdate` 安全化、`HasDownloadFailed`/`FailureTip`/`RetryDownloadCommand`、`BuildUpdateDownloadUrl` 抽取）、`UpdatePage.axaml`（重试下载按钮）。
- **M5 macOS 原生菜单**（落点：主窗口）：`MainWindow.axaml.cs` `NativeMenuBar` 段，命令与主界面同源（`AppShellCommands` / `NavigateToPage`）。
- **M6 系统托盘**（落点：主窗口）：`MainWindow.axaml.cs` `CreateContextMenu` 增加「全部停止隧道」，其余项走 `AppShellCommands`。
- **M6b 流量悬浮窗**（落点：ProxyMonitor）：`ProxyFloatViewModel`（`RefreshTrafficCommand`）、`ProxyFloat.axaml`（MenuFlyout 三项绑定）、`App.axaml.cs`（`desktop.Exit` 时 `ProxyFloat.Instance?.Close()` 无残留）。
- **M7 开机自启与恢复隧道**（落点：设置页 + 主窗口）：`SettingsPage.axaml.cs`（Win 注册表 / Linux .desktop / macOS LaunchAgent）、`MainWindow.axaml.cs` AUTO_START 段（失败写日志 + Growl 提示）。

## 26.3 文档

- `docs/user_guide.md`：隧道管理（测速/状态/失败复制）、创建隧道（预填/端口/模板）、更新（失败/重试/下载源）、桌面集成（macOS 菜单/托盘/悬浮窗/自启）。
- `docs/install.md`：安装 + mefrpc 首次拉取失败路径。
- `docs/troubleshooting.md`：隧道启动失败（认证/端口/节点/崩溃）+ 更新失败排查。
